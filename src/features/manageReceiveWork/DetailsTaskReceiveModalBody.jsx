import { useDispatch } from "react-redux";

function DetailsTaskReceiveModalBody({ closeModal, extraObject }) {
  const dispatch = useDispatch();

  return <div className="h-auto relative"></div>;
}

export default DetailsTaskReceiveModalBody;
